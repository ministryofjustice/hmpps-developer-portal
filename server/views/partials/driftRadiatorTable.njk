

{% macro driftRadiatorTable(params) %}
    <table id="dependencyDriftTable" data-module="moj-sortable-table" class="govuk-table {{ 'govuk-!-margin-top-9' if params.standalone }}">
        <thead>
        <tr>
            <th aria-sort="ascending">Component</th>
            <th aria-sort="none">Staleness</th>
            <th aria-sort="none">Drift</th>
            <th>Environments</th>
        </tr>
        </thead>
        <tbody id="dependencyDriftRows" class="govuk-table__body">
        </tbody>
    </table>
{% endmacro %}
